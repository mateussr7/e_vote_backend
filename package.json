{
  "name": "servidor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma": "npx prisma",
    "prisma:migrate": "npm run prisma migrate dev",
    "prisma:generate": "npm run prisma generate",
    "dev": "npm run prisma:migrate && npm run prisma:generate && node src/index.js",
    "setup": "npm install && npm run prisma:migrate && npm run prisma:generate"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.11.0",
    "@types/cors": "^2.8.13",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.17",
    "@types/jsonwebtoken": "^9.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "prisma": "^4.11.0",
    "sqlite3": "^5.1.4",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.5"
  }
}
